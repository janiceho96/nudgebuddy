version: '3.8'

services:
  nudgebuddy:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nudgebuddy-app
    restart: unless-stopped
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=production
